{% extends 'base.html.twig' %}

{% block title %}Edit Site{% endblock %}

{% block body %}
    <div class="container mb-5 pb-5">
        <div class="row">
            <div class="col-8 offset-2">
                <h1 class="my-4 text-primary">Modifier le chantier</h1>
            </div>
        </div>
        <div class="row mt-4">
            <div class="col-8 offset-2">
                {{ include('site/_form.html.twig', {'button_label': 'Enregistrer'}) }}
            </div>
        </div>
        <div class="row my-4">
            <div class="col-8 offset-2">
                <a href="{{ path('site_index') }}" class="btn btn-primary">Retour à la liste</a>
            </div>
        </div>
    </div>
{% endblock %}
