vich_uploader:
    db_driver: orm

    mappings:
        name_file:
            # alias qui nous permet d'accéder à nos images depuis le navigateur
            uri_prefix: /images/names
            # Chemin vers le dossier de stockage
            upload_destination: '%upload_directory%'